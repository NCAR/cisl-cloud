apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
spec:
  destination:
    name: in-cluster
    namespace: ali-grafana
  source:
    path: cluster/argocd/app-of-apps-test
    repoURL: 'https://github.com/NCAR/cisl-cloud'
    targetRevision: argo-app-of-app
  project: default
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true